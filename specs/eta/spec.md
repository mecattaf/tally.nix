# eta — the daily-driver buildout, chapters one and two

Status: proposed
Governs: silent-factory-worklists/eta.json
Consumers: the eta worklist readFirst anchors; the C1 seam sitting; checks.x86_64-linux.spec-lint once the attribute exists
Supersedes: none

## Outcome

Today the substrate can kill a lane without naming itself and an adapter
switch dies on another adapter's vocabulary, while the authority plane has no
mechanical reader. After chapters one and two merge, a job unit carries no
limit nobody declared, an out-of-memory kill and a quota wall each name
themselves on the durable failure fact, every excerpt bound is a derivation
over its consumer, a policy-less worklist renders against every catalog
adapter, and an unmarked substrate numeral is a build failure. Beside that,
crates/spec-lint parses every committed spec directory on every fleet-gated
head with a bite proof inside the same derivation, and the three campaign
skills carry the spec-layer doctrine as committed bytes. The campaign closes
chapter one and chapter two through checkpoint C1 and continues by worklist
amendment.

## Vocabulary

- vestige — an unauthored substrate constant that presents as agent failure;
  the census lives in specs/substrate/evidence/vestige-sweep.md.
- adapter-terminal (NEW) — an outcome class scraped from the agent stream's
  own terminal error event; it stops the retry ladder.
- substrate-numerals (NEW) — the flake check attribute that scans module and
  adapter-library bytes for resource directives lacking a provenance marker.

## Rulings

| id | decision | ruling |
|---|---|---|
| E1 | campaign shape | one identity, eta; this revision carries chapters one and two plus checkpoint C1; chapters three to five enter by worklist amendment at seams |
| E2 | chapter-two claim source | specs/zeta/spec.md stays the claim authority for the five lint tasks; eta duplicates none of its claims; zeta's trace rows land at the C1 seam sitting |
| E3 | steward | zeta DECISION-1 settles to narrator (given): the diagnosis and judge slots bind the steward catalog role, so evaluation never runs on the metered lane adapter |
| E4 | worklist policy keys | the three agent policy keys are explicitly null until adapter-relative-policies merges and deploys |
| E5 | concurrency | maxParallel stays 1 (given) until a post-calibration amendment; the knob is permanent, and host pool capacity remains the ceiling above it |
| E6 | zeta UNKNOWN-1 | no cargo test covers the read-first brief rendering (verified 2026-08-15 by grep over the campaign CLI); zeta claim 3.2 keeps its HUMAN-ATTENDED binding |

## Claims

### R1 — the substrate repair
Why: fourteen kills and three masquerades in one night traced to constants nobody authored; the cure is deletion, legibility, and a standing guard.
1.1 a job unit rendered with no declared limits → its systemd argv carries no MemoryMax= directive. [gate: cargo-tests]
1.2 the module-rendered daemonArgv → carries no --memory-max-bytes flag. [check: module-layer]
1.3 an exit record whose cgroup oom-kill count is nonzero → classification names the kill with the effective cap. [gate: cargo-tests]
1.4 a capture whose causal error lies above the tail window → the rendered failure fact carries the causal line. [gate: cargo-tests]
1.5 a policy-less worklist admitted against each catalog adapter → the admission argv renders without a cross-adapter literal. [gate: cargo-tests]
1.6 an agent stream ending in a terminal quota event → the outcome envelope carries the adapter-terminal class with the stream's own message text. [gate: cargo-tests]
1.7 an adapter-terminal outcome → the retry ladder dispatches nothing further for that attempt. [gate: cargo-tests]
1.8 a lane trace carrying usage records → the outcome envelope carries the scraped token totals. [gate: cargo-tests]
1.9 a module resource directive without a provenance marker → the substrate-numerals check fails. [check: substrate-numerals]
1.10 the substrate-numerals task merged → the agency-era python driver is absent from the tree. [gate: cargo-tests]

### R2 — the authority plane, joined
Why: the five lint tasks answer to specs/zeta/spec.md; this group claims only the join eta adds.
2.1 BELIEVE:specs/zeta/spec.md — the zeta claim groups anchor at r1 through r4 → the eta worklist's lint-task readFirst anchors resolve. [HUMAN-ATTENDED]
2.2 checkpoint C1 green → the spec-lint check attribute grades every later gated head. [check: spec-lint]

## Unchanged

U.1 worklist admission refuses unknown keys → the eta worklist adds none and admits under schemaVersion 1 (given). [gate: cargo-tests]
U.2 the pre-existing checks module-layer and campaign-runtime build beside the new attributes → the flake stays green. [gate: flake-build-subset]

## Unknowns

Omitted: the campaign's open decisions live in ETA.md with named due seams; none types a spec unknown at this revision.

## Stages

### S1 — chapters one and two
Order: the substrate chain job-limits-optional, then oom-legibility, then
excerpt-derivations; adapter-relative-policies then adapter-terminal-outcomes
beside it; substrate-numerals-guard after job-limits-optional; the lint chain
spec-lint-core, then spec-lint-resolution, then spec-lint-flake-check, then
spec-layer-skills-amend; doc-anchor-regrammar independent; checkpoint
chapter-gate-c1 closes over all of them. Claims R1, R2; rulings E1 to E6.

### S2 — chapters three to five
Unauthored. Rulings E1.

## Forbidden

F.1 Do not add keys to the worklist schema.
F.2 Do not put model names in spec or governing worklist bytes.
F.3 Never write under specs/eta/ from any lane.
F.4 Never write under specs/zeta/ from any lane.
F.5 Do not edit test/fleet-gate.sh.
F.6 Do not reintroduce a required unit limit.
F.7 Do not bake adapter vocabulary into adapter-neutral defaults.
F.8 Do not delete the maxParallel worklist key.
