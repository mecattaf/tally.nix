# Shared `services.tally.*` options ⚙️

> This page is generated from `nix/modules/common.nix` by `nix build .#doc`.
> Do not edit the rendered page.

This is the typed option tree shared by both module wrappers. The shared module takes its
package and directory defaults from the importing wrapper, so use the
[Home Manager reference](home-manager-options.md) or the
[NixOS reference](nixos-options.md) when choosing between those deployment surfaces.
