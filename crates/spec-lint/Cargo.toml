[package]
name = "spec-lint"
version.workspace = true
edition.workspace = true
license.workspace = true
description.workspace = true
repository.workspace = true
keywords.workspace = true
readme = "README.md"

[dependencies]
anyhow.workspace = true
clap.workspace = true
regex.workspace = true
# The cross-artifact half reads the governing worklist and trace.json at lint
# runtime, so serde_json moves out of dev-dependencies. Cargo.lock already
# carries it for this crate: promoting a dev-dependency changes no lock byte.
serde_json.workspace = true
