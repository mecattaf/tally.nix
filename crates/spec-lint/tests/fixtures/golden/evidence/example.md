# The golden fixture's evidence note

One graded line, so the `golden fixture` has a believed path that resolves and
a file whose bytes carry the identifier its claim line quotes.

| finding | grade | note |
|---|---|---|
| E1 | observed | the corpus test reads this file through the BELIEVE mark on claim 1.2 |
